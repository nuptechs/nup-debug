hardening round 3: path traversal, CORS, SSL, auth guard, timing-safe keys

CRITICAL:
- All :id route params validated via sessionIdSchema (regex /^[\w-]+$/)
  prevents path traversal in FileStorageAdapter

HIGH:
- CORS: deny all origins in production when CORS_ORIGINS is unset
- SSL: default to rejectUnauthorized: true (was false - MITM vector)
- PROBE_AUTH_DISABLED=1 now kills process in production (was silently honored)

MEDIUM:
- API key comparison now uses timing-safe constant-time check
- WebSocket connections drained on graceful shutdown (wss returned + closed)
- Timeline/groups rebuild capped at 50K events (matches correlator cap)
- Report error handler no longer leaks internal details in any environment

10/10 builds, 188/188 tests
