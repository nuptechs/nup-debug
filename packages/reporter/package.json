{
  "name": "@nuptechs-sentinel-probe/reporter",
  "version": "0.1.0",
  "type": "module",
  "description": "Debug report generation — HTML, JSON, and Markdown formats",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@nuptechs-sentinel-probe/core": "*",
    "@nuptechs-sentinel-probe/correlation-engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
