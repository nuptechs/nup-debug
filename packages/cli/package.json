{
  "name": "@nuptechs-sentinel-probe/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "probe": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@nuptechs-sentinel-probe/core": "*",
    "@nuptechs-sentinel-probe/browser-agent": "*",
    "@nuptechs-sentinel-probe/log-collector": "*",
    "@nuptechs-sentinel-probe/network-interceptor": "*",
    "@nuptechs-sentinel-probe/correlation-engine": "*",
    "@nuptechs-sentinel-probe/reporter": "*",
    "@nuptechs-sentinel-probe/sdk": "*",
    "commander": "^12.1.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
